# dependencias
node_modules/

# variables de entorno — NUNCA subir (contienen la API key)
.env
.env.local
.env*.local

# vercel
.vercel/

# sistema
.DS_Store
Thumbs.db

# lienzo de diseño (no forma parte de la web publicada)
design2/

# ficheros de trabajo de la sesión (servidor local y mensajes de commit)
.serve.mjs
.msg.txt
